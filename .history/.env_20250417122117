# Configuration de la base de données
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=testdb

# Configuration de l'application
PORT=3001

# Secret JWT (actuellement commenté)
# JWT_SECRET="your-secret-key"

# URL complète de la base de données (peut être conservée comme référence)
#DATABASE_URL="postgresql://root:root@localhost:5432/testdb"